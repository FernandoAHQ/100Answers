import React from "react";
import styles from "../pages/host/host.module.css";

function Loading() {
  return (
    <div>
      <h1 className={styles.waitingTitle}>Loading</h1>
    </div>
  );
}

export default Loading;
